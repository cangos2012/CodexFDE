# 操作手册与实验检查记录｜2026-09-15

## 当前结论

L01 的未初始化分流已补齐；继续检查 L02～L16 后，修订 14 份手册与公共排错说明，并修复实验脚本对控制仓库旧 `flowerp/` 路径的依赖。L13 手册原文保留，HTTP 与重启实验已实际运行。

Windows 与 WSL 各验证 **118 个本地固定实验模式**，正常与预期故障的退出码均符合预期。新增 8 项集成回归覆盖源码与解释器边界、L04 文件交付、L05/L06 实验衔接、L09 失败映射、L14 API/SQLite 重启一致性和 L15 摘要导出。原故障日志保留。最终全量单元测试运行 418 项，416 项通过、2 项本地 PPT 检查按默认配置跳过；工作台 blocking Eval 为 10/10 通过。

验证环境：Windows Python 3.11.15；WSL Ubuntu Python 3.14.4。客户源码基线与 CI 一致，为 `e0088d3a11ae68b7e23c2c6986167b87768d909e`。

## 修复内容

| 范围 | 当前行为与验证 |
|---|---|
| L01～L03 环境衔接 | 未初始化有明确下一步；隔离区可借用已确认解释器，同时检查实际源码来源 |
| L04 | 补实际成果路径、项目登记、失败停止和任务分流；文件交付五类场景及重复输出拒绝已跑 |
| L05 | 缺陷副本取自独立客户源码；旧检查绿、完整检查两次红、修复后绿；工程范围正常/故障均跑 |
| L06 | 两仓库组合隔离候选并记录来源哈希；假绿、门禁修复、业务修复、七行迁移、续做、旧报告拒绝均跑；兼容准备器来源也已修正 |
| L07～L12 | 业务观察转到客户解释器；控制、订单、取消、循环、整合、采购各固定模式均跑；候选业务 Eval 使用副本自身源码 |
| L13～L14 | HTTP 任务与真实进程重启实验已跑；同一事项在服务重启前后与数据库字段一致；补独立终端与运行目录步骤 |
| L15 | RAG 固定模式已跑；摘要读取真实未验收任务，保持状态与缺失证据，拒绝覆盖及错误指纹 |
| L16 | 恢复、错误指纹拒绝与源库保护实验已跑；补 Graph 待人审的后续步骤 |

共享适配器为 `workbench/course_experiments.py`。实验副本中的 `.course/experiment-source.json` 记录原文件路径和 SHA-256；它记录复制来源，后续教学缺陷与学生修改需另看 Diff。没有在控制仓库恢复业务包，没有修改独立 FlowERP 源码。

## 如何复验

按[公共排错说明的开课前批量验证](实操手册执行与排错.md)安装并检查两个仓库，然后从 CodexFDE 根目录运行：

```text
python -B -X utf8 scripts/audit_course_experiments.py --output-dir .runtime/course-labs-check-01
python -B -X utf8 -m unittest tests.test_course_experiments -v
```

两条命令都应退出 0。第一条末尾应为 `total: 118`、`failed: []`；每个故障模式自己的非零退出码会保留在记录中。第二条应为 8 项通过。重跑使用新的输出目录；局部复查可添加 `--lesson 9`。

CI 已增加同一批量实验步骤并上传进程 JSON 证据；数据库与候选目录不在该制品范围内。此次尚未触发远程 CI，不能把本地通过写成远程已通过。

本机证据（运行产物不提交 Git，其他机器按以上命令生成自己的记录）：

- [Windows 最终 118 个模式](../../.runtime/experiments-final-118/summary.json)。
- [WSL 最终 118 个模式](../../.runtime/experiments-wsl-summary-final.json)与[8 项集成回归输出](../../.runtime/experiments-wsl-integration-final.log)。
- [Windows 8 项集成回归输出](../../.runtime/experiments-integration-final.log)。每次子进程原始输出另存于 `.runtime/experiment-regression/` 的独立目录。
- [最终全量单元测试输出](../../.runtime/experiments-full-unittest-final.log)。首次运行的新增 L15 测试夹具把缺失 Spec 指向运行目录外，现已改为运行目录内的缺失文件，并复验其拒绝与缺证据行为；首轮日志保留于 `.runtime/experiments-full-unittest.log`。
- [工作台 blocking Eval](../../.runtime/experiments-blocking-final.json)。

## 验证范围

上述检查覆盖本机 Windows 与 WSL 的正常、预期故障与重点续做路径。WSL 使用按文件记录哈希的两仓库源码快照、独立 Git 目录和两套 Linux 虚拟环境；第一次快照遗漏 Git 目录导致 L13/L14 工具检查拒绝，补齐环境后全部复验通过。WSL 进程记录与来源清单保存在 `.runtime/experiments-wsl-evidence.zip`，不包含数据库。维护夹具明确标记用途，不是学生成果。L01～L03 构建过程、各讲实际 Codex 编码、真实 Hook 安装、浏览器人工操作、远程 CI 和具名审核仍须学员按各讲完成；不能由固定模式结果替代。此次没有把这些外部或人工步骤登记为已完成，也未同步 Word/PPT。

## 修复前故障记录（已保留）

执行环境：Windows PowerShell，控制目录 `D:\work\CodexFDE`，使用该目录 `.venv\Scripts\python.exe -B -X utf8`。未向控制环境安装 FlowERP。

以下 6 个入口以 `--help` 探测，均在参数处理或业务运行前因 `ModuleNotFoundError: No module named 'flowerp'` 退出 1。没有取得这些实验的业务结果：

- `docs/courses/L07/examples/order_state_lab.py`
- `docs/courses/L09/examples/cancellation_lab.py`
- `docs/courses/L10/examples/order_transition_lab.py`
- `docs/courses/L11/examples/purchase_request_lab.py`
- `docs/courses/L12/examples/purchase_approval_lab.py`
- `docs/courses/L16/examples/restore_lab.py`

另用本轮独立输出目录执行 3 个复制实验，均因 `D:\work\CodexFDE\flowerp` 不存在退出 1：

- L05：`prepare_receiving_fixture.py --output <本轮新目录>`。
- L10：`candidate_loop_lab.py repair --run-dir <本轮新目录>`。
- L11：`integration_lab.py --run-dir <本轮新目录>`。

上述目录参数是说明占位，不是可原样复制的命令。原始命令参数、退出码和输出保留在本地 [导入探测记录](../../.runtime/manual-audit-20260915-102445/import-probes.json) 与 [复制实验记录](../../.runtime/manual-audit-20260915-102445/copy-probes.json)。这些运行产物不提交 Git；其他机器使用新输出目录复现，不使用本机路径。

修复前，L06 `import_practice.py` 的 `prepare()` 也直接 `copytree(ROOT/name, ...)` 复制 `flowerp`，当时静态确认依赖不存在；首次检查未跑完整 prepare。后续已用标明用途的维护夹具运行准备、假绿、阻断、修复、迁移与续做，记录见下方。
